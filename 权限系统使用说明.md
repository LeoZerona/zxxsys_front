# 权限系统使用说明

## 概述

本系统实现了基于后端返回的权限和菜单数据的前端权限控制系统，支持：
- 不同用户展示不同的菜单
- 不同用户展示不同的功能权限
- 没有权限的菜单和功能不展示
- 权限过滤功能分为三种环境（开发、测试、生产），可以随时启用和关闭

## 功能特性

### 1. 菜单权限控制

系统会根据登录接口返回的 `menus` 动态生成路由和菜单。只有用户有权限的菜单才会显示。

### 2. 按钮级权限控制

提供了两种方式控制按钮权限：

#### 方式一：使用指令 `v-permission`

```vue
<template>
  <!-- 单个权限 -->
  <el-button v-permission="'questionBank:edit'">编辑</el-button>
  
  <!-- 多个权限（任意一个满足即可） -->
  <el-button v-permission="['questionBank:edit', 'questionBank:view']">操作</el-button>
</template>
```

#### 方式二：使用组件 `<PermissionWrapper>`

```vue
<template>
  <PermissionWrapper permission="questionBank:edit">
    <el-button>编辑</el-button>
  </PermissionWrapper>
  
  <!-- 需要所有权限 -->
  <PermissionWrapper permission="['questionBank:edit', 'questionBank:delete']" :require-all="true">
    <el-button>删除</el-button>
  </PermissionWrapper>
</template>
```

#### 方式三：使用工具函数

```vue
<script setup>
import { hasPermission } from '@/utils/permission'

const canEdit = computed(() => hasPermission('questionBank:edit'))
</script>

<template>
  <el-button v-if="canEdit">编辑</el-button>
</template>
```

### 3. 权限过滤配置

权限过滤功能支持三种环境配置，可以随时启用和关闭。

#### 配置方式

1. **通过代码配置**（推荐在开发环境使用）

```typescript
import { setPermissionFilterConfig } from '@/utils/permission'

// 启用权限过滤
setPermissionFilterConfig({ enabled: true, env: 'development' })

// 禁用权限过滤
setPermissionFilterConfig({ enabled: false })
```

2. **通过配置组件**（推荐在测试/生产环境使用）

在需要的地方引入并使用 `PermissionConfig` 组件：

```vue
<template>
  <el-button @click="showConfig = true">权限配置</el-button>
  <PermissionConfig v-model="showConfig" />
</template>

<script setup>
import { ref } from 'vue'
import PermissionConfig from '@/components/PermissionConfig.vue'

const showConfig = ref(false)
</script>
```

#### 配置说明

- **启用权限过滤**：开启后，系统会根据用户权限过滤菜单和功能按钮
- **生效环境**：选择权限过滤在哪个环境生效
  - 开发环境：仅在 `development` 模式下生效
  - 测试环境：仅在 `test` 或 `testing` 模式下生效
  - 生产环境：仅在 `production` 模式下生效

**注意**：如果当前环境与配置的环境不匹配，权限过滤将不会生效（所有菜单和功能都会显示）。

## 后端接口要求

### 登录接口返回格式

登录接口 `POST /api/login` 需要返回以下字段：

```json
{
  "success": true,
  "data": {
    "user": {
      "id": 1,
      "email": "user@example.com",
      "role": "user"
    },
    "access_token": "xxx",
    "refresh_token": "yyy",
    "token_type": "Bearer",
    "expires_in": 3600,
    "permissions": ["test:view", "questionBank:view", "questionBank:detail"],
    "menus": [
      {
        "path": "/second",
        "name": "second",
        "component": "@/pages/second/index.vue",
        "meta": {
          "requiresAuth": true,
          "title": "主页面",
          "icon": "home",
          "hidden": false
        },
        "children": [
          {
            "path": "test",
            "name": "test",
            "component": "@/pages/test/index.vue",
            "meta": {
              "requiresAuth": true,
              "title": "测试页",
              "icon": "test",
              "hidden": false
            },
            "permissions": ["test:view"]
          }
        ]
      }
    ]
  }
}
```

### 字段说明

- `permissions`: 字符串数组，用户拥有的操作权限
- `menus`: 菜单树，已按用户角色/权限过滤
  - `path`: 路由路径
  - `name`: 路由名称
  - `component`: 组件路径（使用 `@/` 别名）
  - `meta`: 路由元信息
    - `requiresAuth`: 是否需要认证（默认 true）
    - `title`: 菜单标题
    - `icon`: 菜单图标
    - `hidden`: 是否隐藏菜单
  - `permissions`: 该菜单项需要的权限（可选）
  - `children`: 子菜单（可选）

## 使用示例

### 示例1：在表格操作列中使用权限

```vue
<template>
  <el-table :data="tableData">
    <el-table-column label="操作">
      <template #default="{ row }">
        <el-button 
          v-permission="'questionBank:edit'"
          @click="handleEdit(row)"
        >
          编辑
        </el-button>
        <el-button 
          v-permission="'questionBank:delete'"
          type="danger"
          @click="handleDelete(row)"
        >
          删除
        </el-button>
      </template>
    </el-table-column>
  </el-table>
</template>
```

### 示例2：在工具栏中使用权限

```vue
<template>
  <div class="toolbar">
    <PermissionWrapper permission="questionBank:add">
      <el-button type="primary" @click="handleAdd">添加</el-button>
    </PermissionWrapper>
    
    <PermissionWrapper permission="questionBank:export">
      <el-button @click="handleExport">导出</el-button>
    </PermissionWrapper>
  </div>
</template>

<script setup>
import PermissionWrapper from '@/components/PermissionWrapper.vue'
</script>
```

### 示例3：在页面中使用权限判断

```vue
<script setup>
import { computed } from 'vue'
import { hasPermission } from '@/utils/permission'

const canEdit = computed(() => hasPermission('questionBank:edit'))
const canDelete = computed(() => hasPermission('questionBank:delete'))

function handleAction(action: string) {
  if (action === 'edit' && !canEdit.value) {
    ElMessage.warning('您没有编辑权限')
    return
  }
  if (action === 'delete' && !canDelete.value) {
    ElMessage.warning('您没有删除权限')
    return
  }
  // 执行操作...
}
</script>
```

## 注意事项

1. **组件路径映射**：后端返回的组件路径需要在 `src/router/index.ts` 的 `componentMap` 中注册，否则路由无法正确加载。

2. **权限标识格式**：建议使用 `模块:操作` 的格式，如 `questionBank:view`、`questionBank:edit` 等。

3. **超级管理员**：如果用户角色为 `super_admin` 或权限列表包含 `*`，则拥有所有权限。

4. **环境配置**：权限过滤配置保存在 `localStorage` 中，关闭浏览器后仍然保留。

5. **菜单缓存**：菜单数据会保存在 `localStorage` 中，页面刷新后会自动恢复。

## 开发建议

1. **开发阶段**：建议禁用权限过滤，方便开发调试
2. **测试阶段**：建议启用权限过滤，测试不同角色的权限控制
3. **生产环境**：必须启用权限过滤，确保安全性

## 常见问题

### Q: 为什么菜单没有显示？

A: 请检查：
1. 登录接口是否返回了 `menus` 字段
2. 菜单是否被权限过滤掉（检查 `permissions` 字段）
3. 权限过滤配置是否启用且环境匹配

### Q: 为什么按钮没有隐藏？

A: 请检查：
1. 是否正确使用了 `v-permission` 指令或 `PermissionWrapper` 组件
2. 权限标识是否正确
3. 用户是否拥有该权限
4. 权限过滤配置是否启用且环境匹配

### Q: 如何临时禁用权限过滤？

A: 可以通过以下方式：
1. 使用 `PermissionConfig` 组件，将"启用权限过滤"设置为关闭
2. 或者在代码中调用 `setPermissionFilterConfig({ enabled: false })`

