# 数据库状态字段修复说明

## 🔴 问题描述

暂停任务时出现数据库错误：
```
(pymysql.err.DataError) (1265, "Data truncated for column 'status' at row 1")
```

**错误原因**：数据库中的 `dedup_tasks` 表的 `status` 字段不支持 `'paused'` 状态值。

---

## 🔧 解决方案

### 方法一：修改 ENUM 字段（推荐）

如果 `status` 字段是 ENUM 类型，需要添加 `'paused'` 值。

#### 1. 检查当前字段定义

```sql
SHOW COLUMNS FROM dedup_tasks WHERE Field = 'status';
```

或者：

```sql
DESC dedup_tasks;
```

#### 2. 修改 ENUM 字段

**MySQL 5.7+ / MariaDB 10.2+**：

```sql
ALTER TABLE dedup_tasks 
MODIFY COLUMN status ENUM(
    'pending',
    'running',
    'paused',      -- 新增这个状态
    'completed',
    'error',
    'cancelled'
) NOT NULL DEFAULT 'pending';
```

**如果字段有注释**：

```sql
ALTER TABLE dedup_tasks 
MODIFY COLUMN status ENUM(
    'pending',
    'running',
    'paused',
    'completed',
    'error',
    'cancelled'
) NOT NULL DEFAULT 'pending' COMMENT '任务状态：pending=待启动, running=运行中, paused=已暂停, completed=已完成, error=错误, cancelled=已取消';
```

---

### 方法二：修改为 VARCHAR 类型（如果当前是 ENUM）

如果希望更灵活，可以将字段改为 VARCHAR：

```sql
-- 先备份数据（可选）
CREATE TABLE dedup_tasks_backup AS SELECT * FROM dedup_tasks;

-- 修改字段类型
ALTER TABLE dedup_tasks 
MODIFY COLUMN status VARCHAR(20) NOT NULL DEFAULT 'pending';

-- 添加检查约束（可选，MySQL 8.0+）
ALTER TABLE dedup_tasks 
ADD CONSTRAINT chk_status 
CHECK (status IN ('pending', 'running', 'paused', 'completed', 'error', 'cancelled'));
```

---

## 📋 验证步骤

### 1. 检查字段定义

```sql
SHOW COLUMNS FROM dedup_tasks WHERE Field = 'status';
```

应该看到 `paused` 在 ENUM 值列表中。

### 2. 测试插入 paused 状态

```sql
-- 测试更新（使用现有任务ID）
UPDATE dedup_tasks 
SET status = 'paused' 
WHERE id = 1 
LIMIT 1;

-- 检查结果
SELECT id, status FROM dedup_tasks WHERE id = 1;

-- 恢复原状态（如果需要）
UPDATE dedup_tasks 
SET status = 'running' 
WHERE id = 1 
LIMIT 1;
```

### 3. 测试前端功能

1. 启动一个任务（状态变为 `running`）
2. 点击"暂停"按钮
3. 应该成功暂停，状态变为 `paused`
4. 点击"继续"按钮
5. 应该成功恢复，状态变为 `running`

---

## 🔍 后端代码检查

确保后端代码中的状态定义包含 `paused`：

### Python SQLAlchemy 示例

```python
from sqlalchemy import Column, Enum
import enum

class TaskStatus(str, enum.Enum):
    PENDING = "pending"
    RUNNING = "running"
    PAUSED = "paused"      # 确保有这个状态
    COMPLETED = "completed"
    ERROR = "error"
    CANCELLED = "cancelled"

class DedupTask(Base):
    __tablename__ = 'dedup_tasks'
    
    status = Column(
        Enum(TaskStatus),
        default=TaskStatus.PENDING,
        nullable=False
    )
```

### Flask-SQLAlchemy 示例

```python
class DedupTask(db.Model):
    __tablename__ = 'dedup_tasks'
    
    status = db.Column(
        db.Enum('pending', 'running', 'paused', 'completed', 'error', 'cancelled'),
        default='pending',
        nullable=False
    )
```

---

## ⚠️ 注意事项

1. **备份数据**：在执行 ALTER TABLE 之前，建议先备份数据
2. **生产环境**：在生产环境执行前，先在测试环境验证
3. **锁表时间**：ALTER TABLE 可能会锁表，建议在低峰期执行
4. **索引影响**：如果 `status` 字段有索引，修改后索引会自动更新

---

## 🚀 快速修复脚本

创建一个 SQL 脚本文件 `fix_dedup_task_status.sql`：

```sql
-- 修复 dedup_tasks 表的 status 字段，添加 paused 状态
-- 执行前请先备份数据库！

-- 方法一：修改 ENUM（推荐）
ALTER TABLE dedup_tasks 
MODIFY COLUMN status ENUM(
    'pending',
    'running',
    'paused',
    'completed',
    'error',
    'cancelled'
) NOT NULL DEFAULT 'pending';

-- 验证
SHOW COLUMNS FROM dedup_tasks WHERE Field = 'status';
```

执行脚本：

```bash
mysql -u your_username -p your_database < fix_dedup_task_status.sql
```

---

## ✅ 修复后验证清单

- [ ] 数据库字段定义已更新，包含 `paused` 状态
- [ ] 可以成功插入/更新 `paused` 状态
- [ ] 前端暂停功能正常工作
- [ ] 前端继续功能正常工作
- [ ] WebSocket 状态推送正常（如果使用）

---

**修复完成后，前端的暂停和继续功能应该可以正常工作了！**

