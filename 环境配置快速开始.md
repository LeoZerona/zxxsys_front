# 环境配置快速开始指南

## ✅ 已完成的工作

我已经为您创建了完整的多环境配置系统，包括：

1. ✅ **环境配置文件**
   - `.env` - 默认配置
   - `.env.development` - 开发环境
   - `.env.test` - 测试环境
   - `.env.production` - 生产环境

2. ✅ **代码更新**
   - `vite.config.ts` - 支持从环境变量读取配置
   - `package.json` - 添加了不同环境的构建命令
   - `src/utils/request.ts` - 使用环境变量配置 API 地址
   - `src/main.ts` - 添加环境信息显示
   - `src/vite-env.d.ts` - TypeScript 类型定义

3. ✅ **文档**
   - `环境配置说明.md` - 详细配置文档
   - `.gitignore` - 已更新，保护敏感配置

## 🚀 您需要做的事情

### 1. 修改环境配置文件中的实际地址

请根据您的实际情况，修改以下文件中的 API 地址：

#### 开发环境 (`.env.development`)
```env
VITE_API_TARGET=http://192.168.0.101:5000  # 修改为您的开发环境后端地址
```

#### 测试环境 (`.env.test`)
```env
VITE_API_BASE_URL=http://test-api.example.com/api  # 修改为您的测试环境 API 地址
VITE_API_TARGET=http://test-api.example.com        # 修改为您的测试环境后端地址
```

#### 生产环境 (`.env.production`)
```env
VITE_API_BASE_URL=http://192.168.0.101:5000/api  # 修改为您的生产环境 API 地址
VITE_API_TARGET=http://192.168.0.101:5000        # 修改为您的生产环境后端地址
```

### 2. 测试不同环境

#### 开发环境（默认）
```bash
npm run dev
```
系统会自动使用 `.env.development` 配置

#### 测试环境
```bash
npm run dev:test
```
系统会自动使用 `.env.test` 配置

#### 生产环境构建
```bash
npm run build        # 生产环境
npm run build:test   # 测试环境
npm run build:dev    # 开发环境（用于调试）
```

### 3. 验证配置是否生效

启动项目后，在浏览器控制台会看到类似以下信息：

```
=== 环境配置信息 ===
环境模式: development
应用环境: development
API Base URL: /api
请求超时: 10000ms
==================
```

如果看到这些信息，说明环境配置已成功加载！

## 📋 环境变量优先级

1. **命令行参数** > **环境文件** > **默认值**
2. `.env.[mode]` 会覆盖 `.env` 中的同名变量
3. 系统会根据运行模式自动选择对应的配置文件

## 🔍 如何确认当前环境

在代码中可以通过以下方式获取当前环境：

```typescript
// 获取环境模式
console.log(import.meta.env.MODE)  // 'development' | 'test' | 'production'

// 获取应用环境
console.log(import.meta.env.VITE_APP_ENV)

// 获取 API 地址
console.log(import.meta.env.VITE_API_BASE_URL)

// 判断是否为开发环境
console.log(import.meta.env.DEV)  // true | false
```

## ⚠️ 重要提示

1. **环境变量命名**：所有自定义环境变量必须以 `VITE_` 开头
2. **修改后重启**：修改 `.env` 文件后需要重启开发服务器
3. **敏感信息**：不要将密码、密钥等敏感信息直接写在配置文件中
4. **版本控制**：根据项目需要决定是否将 `.env` 文件提交到 Git

## 📚 更多信息

详细配置说明请查看：[环境配置说明.md](./环境配置说明.md)

## 🆘 遇到问题？

1. **环境变量不生效**：检查变量名是否以 `VITE_` 开头
2. **API 地址错误**：检查对应环境的 `.env` 文件配置
3. **构建失败**：检查环境变量语法是否正确（不要有多余的空格）

