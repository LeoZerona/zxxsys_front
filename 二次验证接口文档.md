# 题目去重二次验证接口文档

## 接口概述

`/api/dedup/tasks/{task_id}/reverify` 接口用于对已完成的去重任务进行二次验证，过滤误判的重复题目对，并可选择重置任务状态让其重新运行。

## 接口信息

- **URL**: `/api/dedup/tasks/{task_id}/reverify`
- **方法**: POST
- **认证**: 需要管理员权限
- **内容类型**: application/json

## 请求参数

### 路径参数

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| task_id | integer | 是 | 去重任务ID |

### 请求体参数

| 参数名 | 类型 | 必填 | 默认值 | 说明 |
|--------|------|------|--------|------|
| verification_type | string | 否 | "similar" | 验证类型：<br>- "similar": 只验证相似重复对<br>- "all": 验证所有重复对 |
| reset_task | boolean | 否 | true | 是否重置任务状态让其重新运行 |
| batch_size | integer | 否 | 50 | 批处理大小（1-200） |

## 验证规则

### 单选题/多选题（type=1,2）
- **验证条件**: 题目内容相似 + 选项完全一致
- **验证逻辑**: 比较所有选项内容，顺序和内容必须完全匹配

### 填空题（type=4）
- **验证条件**: 题目内容相似 + 答案完全一致
- **验证逻辑**: 比较答案内容（去除空白字符）

### 判断题/计算分析题（type=3,8）
- **验证条件**: 只需要题目内容相似
- **验证逻辑**: 直接通过验证

## 响应格式

### 成功响应 (200)

```json
{
    "success": true,
    "message": "二次验证完成，共验证 150 对重复题目，保留 120 对，过滤 30 对，任务已重置为待运行状态",
    "data": {
        "total_pairs": 150,
        "verified_pairs": 120,
        "filtered_pairs": 30,
        "task_reset": true,
        "verification_type": "similar"
    }
}
```

### 错误响应

#### 任务不存在 (404)
```json
{
    "success": false,
    "message": "任务不存在",
    "error_code": "NOT_FOUND"
}
```

#### 任务状态无效 (400)
```json
{
    "success": false,
    "message": "任务状态为 running，只有已完成的任务才能进行二次验证",
    "error_code": "INVALID_STATUS"
}
```

#### 参数无效 (400)
```json
{
    "success": false,
    "message": "验证类型无效，支持的类型：similar, all",
    "error_code": "INVALID_PARAMETER"
}
```

## 使用示例

### 基本使用

```bash
curl -X POST \
  http://localhost:5000/api/dedup/tasks/123/reverify \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{}'
```

### 自定义参数

```bash
curl -X POST \
  http://localhost:5000/api/dedup/tasks/123/reverify \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -d '{
    "verification_type": "all",
    "reset_task": false,
    "batch_size": 100
  }'
```

### JavaScript (前端调用)

```javascript
// 二次验证任务
async function reverifyTask(taskId) {
    try {
        const response = await fetch(`/api/dedup/tasks/${taskId}/reverify`, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Authorization': `Bearer ${token}`
            },
            body: JSON.stringify({
                verification_type: 'similar',
                reset_task: true,
                batch_size: 50
            })
        });

        const result = await response.json();

        if (result.success) {
            console.log('验证完成:', result.data);
            // 更新UI显示验证结果
            updateVerificationResult(result.data);
        } else {
            console.error('验证失败:', result.message);
            showError(result.message);
        }
    } catch (error) {
        console.error('请求失败:', error);
    }
}

// 带进度显示的验证
async function reverifyWithProgress(taskId) {
    // 显示进度条
    showProgressBar();

    const response = await fetch(`/api/dedup/tasks/${taskId}/reverify`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'Authorization': `Bearer ${token}`
        },
        body: JSON.stringify({
            verification_type: 'similar',
            reset_task: true
        })
    });

    const result = await response.json();

    // 隐藏进度条
    hideProgressBar();

    if (result.success) {
        // 显示验证结果
        displayVerificationSummary(result.data);
    }
}
```

## 处理流程

### 1. 参数验证
- 验证任务ID是否存在
- 检查任务状态（必须是已完成状态）
- 验证请求参数的有效性

### 2. 数据准备
- 根据验证类型构建查询条件
- 统计需要验证的重复对总数

### 3. 批次验证
- 分批处理重复对（默认每批50个）
- 对每个重复对执行类型化验证
- 实时输出处理进度

### 4. 结果处理
- 删除验证失败的重复对记录
- 更新任务的统计信息
- 可选择重置任务状态

### 5. 任务重置（可选）
- 将任务状态重置为"pending"
- 清理进度文件
- 重置处理计数器

## 性能考虑

### 批处理策略
- **默认批大小**: 50个重复对
- **最大批大小**: 200个重复对
- **最小批大小**: 1个重复对

### 进度反馈
- 每处理10个重复对输出一次进度
- 每完成一个批次输出批次进度
- 支持任务中断检查

### 内存管理
- 分批处理避免内存溢出
- 及时提交数据库更改
- 自动清理临时数据

## 错误处理

### 可中断验证
- 支持任务取消/错误状态检查
- 验证过程中可安全中断
- 数据库事务保护数据一致性

### 异常恢复
- 遇到验证错误时跳过该重复对
- 记录错误日志但不中断整个验证过程
- 支持部分成功的结果返回

## 返回数据说明

### 验证结果数据结构

```javascript
{
    total_pairs: 150,        // 验证的重复对总数
    verified_pairs: 120,     // 验证通过的重复对数
    filtered_pairs: 30,      // 被过滤掉的重复对数
    task_reset: true,        // 是否重置了任务状态
    verification_type: "similar"  // 验证类型
}
```

### 统计信息更新

验证完成后会更新以下任务统计信息：
- `similar_duplicate_pairs`: 相似重复对数量
- `updated_at`: 最后更新时间
- `status`: 任务状态（如果重置）

## 注意事项

1. **任务状态要求**: 只有状态为"completed"的任务才能进行二次验证
2. **权限控制**: 需要管理员权限执行验证操作
3. **数据备份**: 建议在验证前备份数据库
4. **性能监控**: 大量重复对的验证可能需要较长时间
5. **并发控制**: 避免对同一任务同时执行多个验证操作

## 相关接口

- `GET /api/dedup/tasks`: 获取任务列表
- `GET /api/dedup/tasks/{task_id}`: 获取任务详情
- `POST /api/dedup/tasks/{task_id}/start`: 启动任务
- `GET /api/dedup/tasks/{task_id}/similar-pairs`: 获取相似重复对

---

**文档版本**: v1.0
**创建日期**: 2025年1月
**最后更新**: 2025年1月