# 环境配置说明

本项目支持多环境配置，系统会根据运行模式自动切换配置。

## 📁 环境配置文件

项目根目录下包含以下环境配置文件：

- `.env` - 默认环境配置（所有环境共享的基础配置）
- `.env.development` - 开发环境配置
- `.env.test` - 测试环境配置
- `.env.production` - 生产环境配置

## 🔧 环境变量说明

### 通用配置（所有环境）

| 变量名                 | 说明                 | 默认值 |
| ---------------------- | -------------------- | ------ |
| `VITE_APP_VERSION`     | 应用版本号           | 1.0.0  |
| `VITE_REQUEST_TIMEOUT` | 请求超时时间（毫秒） | 10000  |

### 环境特定配置

| 变量名                       | 说明                     | 开发环境                  | 测试环境                        | 生产环境                      |
| ---------------------------- | ------------------------ | ------------------------- | ------------------------------- | ----------------------------- |
| `VITE_APP_TITLE`             | 应用标题                 | 题库管理系统-开发环境     | 题库管理系统-测试环境           | 题库管理系统                  |
| `VITE_API_BASE_URL`          | API 基础地址             | /api                      | http://test-api.example.com/api | http://192.168.0.104:5000/api |
| `VITE_API_TARGET`            | 后端服务地址（用于代理） | http://192.168.0.104:5000 | http://test-api.example.com     | http://192.168.0.104:5000     |
| `VITE_APP_ENV`               | 应用环境标识             | development               | test                            | production                    |
| `VITE_USE_MOCK`              | 是否启用 Mock 数据       | false                     | false                           | false                         |
| `VITE_DEBUG`                 | 是否显示调试信息         | true                      | true                            | false                         |
| `VITE_DEBUG_LOG`             | 是否启用调试日志         | false                     | false                           | false                         |
| `VITE_DEBUG_LOG_ENDPOINT`    | 调试日志服务地址         | -                         | -                               | -                             |
| `VITE_DEBUG_LOG_ENDPOINT_ID` | 调试日志服务 ID          | -                         | -                               | -                             |
| `VITE_DEBUG_LOG_SESSION_ID`  | 调试日志会话 ID          | default-session           | default-session                 | default-session               |
| `VITE_DEBUG_LOG_RUN_ID`      | 调试日志运行 ID          | run1                      | run1                            | run1                          |

## 🚀 使用方法

### 开发环境（默认）

```bash
# 启动开发服务器（使用开发环境配置）
npm run dev

# 或者明确指定开发环境
npm run dev --mode development
```

### 测试环境

```bash
# 使用测试环境配置启动开发服务器
npm run dev:test

# 构建测试环境版本
npm run build:test

# 预览测试环境构建
npm run preview:test
```

### 生产环境

```bash
# 构建生产环境版本（默认）
npm run build

# 或者明确指定生产环境
npm run build --mode production
```

## 📝 如何修改配置

### 1. 修改 API 地址

根据不同环境，修改对应的 `.env` 文件：

**开发环境** (`.env.development`):

```env
VITE_API_BASE_URL=/api
VITE_API_TARGET=http://192.168.0.104:5000
```

**测试环境** (`.env.test`):

```env
VITE_API_BASE_URL=http://test-api.example.com/api
VITE_API_TARGET=http://test-api.example.com
```

**生产环境** (`.env.production`):

```env
VITE_API_BASE_URL=http://192.168.0.104:5000/api
VITE_API_TARGET=http://192.168.0.104:5000
```

### 2. 添加新的环境变量

1. 在对应的 `.env` 文件中添加变量，变量名必须以 `VITE_` 开头
2. 在 `src/vite-env.d.ts` 中添加类型定义
3. 在代码中使用 `import.meta.env.VITE_变量名` 访问

示例：

```typescript
// .env.development
VITE_CUSTOM_CONFIG = some_value;

// src/vite-env.d.ts
interface ImportMetaEnv {
  readonly VITE_CUSTOM_CONFIG: string;
}

// 代码中使用
const config = import.meta.env.VITE_CUSTOM_CONFIG;
```

## 🔍 环境识别机制

系统会根据以下规则自动识别环境：

1. **开发环境** (`npm run dev`):

   - 使用 `.env.development` 配置
   - `import.meta.env.MODE = 'development'`
   - `import.meta.env.DEV = true`

2. **测试环境** (`npm run dev:test` 或 `npm run build:test`):

   - 使用 `.env.test` 配置
   - `import.meta.env.MODE = 'test'`
   - `import.meta.env.DEV = false`

3. **生产环境** (`npm run build`):
   - 使用 `.env.production` 配置
   - `import.meta.env.MODE = 'production'`
   - `import.meta.env.PROD = true`

## 🐛 调试日志配置

项目已优化调试日志功能，移除了硬编码的日志代码。如需启用调试日志，请在环境配置文件中添加以下变量：

```env
# 启用调试日志
VITE_DEBUG_LOG=true

# 调试日志服务地址（完整 URL，不包含 ID）
VITE_DEBUG_LOG_ENDPOINT=http://127.0.0.1:7243/ingest

# 调试日志服务 ID
VITE_DEBUG_LOG_ENDPOINT_ID=your-endpoint-id

# 可选：自定义会话 ID 和运行 ID
VITE_DEBUG_LOG_SESSION_ID=debug-session
VITE_DEBUG_LOG_RUN_ID=run1
```

**注意**：

- 默认情况下，调试日志功能是关闭的（`VITE_DEBUG_LOG` 未设置或为 `false`）
- 只有在开发环境且配置完整时，调试日志才会生效
- 调试日志发送失败不会影响主流程（静默失败）
- 生产环境建议关闭调试日志功能

## ⚠️ 注意事项

1. **环境变量命名**：所有自定义环境变量必须以 `VITE_` 开头，否则无法在客户端代码中访问
2. **敏感信息**：不要将敏感信息（如密钥、密码）直接写在 `.env` 文件中，建议使用环境变量注入
3. **配置文件优先级**：`.env.[mode]` 会覆盖 `.env` 中的同名变量
4. **修改后重启**：修改环境变量后需要重启开发服务器才能生效
5. **调试日志**：调试日志功能已优化，不再使用硬编码的 ID 和 URL，请通过环境变量配置

## 📦 部署说明

### 开发环境部署

```bash
npm run build:dev
```

### 测试环境部署

```bash
npm run build:test
```

### 生产环境部署

```bash
npm run build
```

构建后的文件在 `dist` 目录，可以直接部署到服务器。

## 🔐 安全建议

1. 将 `.env` 文件添加到 `.gitignore`，避免敏感信息泄露
2. 在 CI/CD 中通过环境变量注入敏感配置
3. 生产环境不要开启 `VITE_DEBUG`
4. 定期检查环境配置文件，确保没有泄露敏感信息

## 📚 相关文档

- [Vite 环境变量文档](https://cn.vitejs.dev/guide/env-and-mode.html)
- [项目 README](./README.md)
