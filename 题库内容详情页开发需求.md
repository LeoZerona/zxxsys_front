# 题库内容详情页开发需求

## 一、功能概述

在原题库页面（`src/pages/originalQuestionBank/index.vue`）中，点击"查看题库内容"按钮后，需要跳转到一个新的详情页面，该页面显示该题库下的所有题目列表。

## 二、路由配置

### 2.1 路由结构
- **路由路径**: `/questionBankDetail/:id` 或 `/questionBankDetail?id=xxx`
- **路由名称**: `questionBankDetail`
- **路由位置**: 在 `src/router/index.ts` 中，作为 `second` 路由的 children 子路由添加
- **路由参数**: 需要传递题库ID（`id`）用于查询该题库下的题目

### 2.2 路由配置示例
```typescript
// 题库内容详情页
{
  path: "/questionBankDetail/:id",
  name: "questionBankDetail",
  component: () => import("@/pages/originalQuestionBank/detail/index.vue"),
  props: true,
},
// 题目类型专门页面
{
  path: "/questionTypeDetail/:bankId/:type",
  name: "questionTypeDetail",
  component: () => import("@/pages/originalQuestionBank/questionType/index.vue"),
  props: true,
}
```

### 2.3 路由参数说明
- `questionBankDetail` 路由参数：
  - `id`: 题库ID（必填）
- `questionTypeDetail` 路由参数：
  - `bankId`: 题库ID（必填）
  - `type`: 题目类型（必填），可选值：`single`、`multiple`、`fill`、`shortAnswer`、`judge`、`essay`

## 三、文件结构

### 3.1 需要创建的文件
```
src/
  pages/
    originalQuestionBank/
      detail/
        index.vue                    # 题库内容详情页主文件
        components/
          QuestionDetailDialog.vue   # 题目详情对话框组件
          QuestionEditDialog.vue     # 题目编辑对话框组件
      questionType/
        index.vue                    # 题目类型专门页面（显示特定类型的题目）
```

### 3.2 文件说明
- `src/pages/originalQuestionBank/detail/index.vue`: 题库内容详情页组件，包含题目列表表格和相关功能
- `src/pages/originalQuestionBank/detail/components/QuestionDetailDialog.vue`: 题目详情查看对话框组件
- `src/pages/originalQuestionBank/detail/components/QuestionEditDialog.vue`: 题目编辑对话框组件
- `src/pages/originalQuestionBank/questionType/index.vue`: 题目类型专门页面，只显示指定类型的题目

## 四、页面功能需求

### 4.1 页面布局
1. **顶部导航栏**：
   - 显示面包屑导航：`首页 / 原题库 / [题库名称]`
   - 提供"返回"按钮，点击后返回原题库页面（`originalQuestionBank`）

2. **工具栏区域**：
   - 使用 `TableToolBar` 组件（`src/components/tableToolBar/index.vue`）
   - 包含：添加、修改、删除、导入、导出、搜索、高级筛选、列设置等功能
   - 搜索功能：对题目内容（题干）进行全文搜索

3. **表格区域**：
   - 使用 `el-table` 组件显示题目列表
   - 支持分页功能
   - 表格样式与原题库页面保持一致

### 4.2 表格字段设计

根据题库题目的特点，建议包含以下字段：

#### 必填字段：
1. **题目编号** (`questionId`)
   - 类型：数字
   - 宽度：100px
   - 说明：题目的唯一标识

2. **科目** (`subject`)
   - 类型：文本/枚举
   - 宽度：120px
   - 说明：题目所属的科目，如：数学、语文、英语、物理、化学、生物等
   - 筛选类型：select（下拉选择）
   - 搜索字段：是

3. **题目内容** (`content`)
   - 类型：文本
   - 宽度：自适应（minWidth: 300px）
   - 说明：题目的题干内容，支持长文本显示
   - 搜索字段：是（主要搜索字段）

4. **题目类型** (`questionType`)
   - 类型：枚举
   - 宽度：120px
   - 可选值：
     - `single` - 单选题
     - `multiple` - 多选题
     - `fill` - 填空题
     - `shortAnswer` - 简答题
     - `judge` - 判断题
     - `essay` - 论述题
   - 筛选类型：select（下拉选择）

4. **难度等级** (`difficulty`)
   - 类型：枚举
   - 宽度：100px
   - 可选值：
     - `easy` - 简单
     - `medium` - 中等
     - `hard` - 困难
   - 筛选类型：select

5. **分值** (`score`)
   - 类型：数字
   - 宽度：80px
   - 说明：题目的分值
   - 筛选类型：input（数字输入）

6. **创建时间** (`createdAt`)
   - 类型：日期时间
   - 宽度：180px
   - 说明：题目创建时间
   - 筛选类型：dateRange（日期范围）

7. **更新时间** (`updatedAt`)
   - 类型：日期时间
   - 宽度：180px
   - 说明：题目最后更新时间
   - 筛选类型：dateRange

8. **操作** (`action`)
   - 类型：操作按钮组
   - 宽度：200px
   - 按钮：
     - "查看详情" - 查看题目完整信息（打开详情对话框或详情页）
     - "编辑" - 编辑题目（打开编辑对话框或编辑页）
     - "删除" - 删除题目
     - "查看类型题目" - 进入该题目类型的专门页面，只显示该类型的题目

#### 可选字段（根据实际需求添加）：
- **知识点** (`knowledgePoint`) - 题目所属知识点
- **标签** (`tags`) - 题目标签（多选）
- **使用次数** (`usageCount`) - 题目被使用的次数
- **正确率** (`correctRate`) - 题目答题正确率
- **状态** (`status`) - 题目状态（启用/禁用）

### 4.3 搜索功能

1. **普通搜索**：
   - 搜索字段：题目内容（`content`）
   - 支持模糊匹配
   - 实时搜索或点击搜索按钮触发

2. **高级筛选**：
   - 题目类型：下拉选择（单选题、多选题、填空题、简答题、判断题、论述题）
   - 难度等级：下拉选择（简单、中等、困难）
   - 分值范围：数字输入
   - 创建时间：日期范围选择
   - 更新时间：日期范围选择

### 4.4 数据交互

1. **获取题目列表**：
   - API 接口：根据题库ID获取该题库下的所有题目
   - 请求参数：`{ bankId: string, page: number, pageSize: number, keyword?: string, ...filters }`
   - 响应数据：`{ list: QuestionItem[], total: number }`

2. **题目数据结构**：
```typescript
interface QuestionItem {
  questionId: number | string;
  subject: string;           // 科目（必填）
  content: string;           // 题目内容
  questionType: 'single' | 'multiple' | 'fill' | 'shortAnswer' | 'judge' | 'essay';
  difficulty: 'easy' | 'medium' | 'hard';
  score: number;
  createdAt: string | Date;
  updatedAt: string | Date;
  // 详细信息（用于详情和编辑）
  options?: string[];        // 选择题选项
  correctAnswer: string;     // 正确答案
  analysis?: string;          // 题目解析
  // 其他可选字段...
}
```

## 五、交互逻辑

### 5.1 页面跳转
1. **从原题库进入**：
   - 在原题库页面点击"查看题库内容"按钮
   - 调用 `handleView(row)` 函数
   - 使用 `router.push()` 跳转到详情页，传递题库ID
   - 示例：`router.push({ name: 'questionBankDetail', params: { id: row.id } })`

2. **返回原题库**：
   - 点击页面顶部的"返回"按钮
   - 使用 `router.push({ name: 'originalQuestionBank' })` 或 `router.back()`
   - 保持原题库页面的筛选和分页状态（可选）

3. **进入题目类型页面**：
   - 在题目列表页面点击"查看类型题目"按钮
   - 跳转到题目类型专门页面，路由：`/questionTypeDetail/:bankId/:type`
   - 该页面只显示指定类型的题目
   - 示例：`router.push({ name: 'questionTypeDetail', params: { bankId: 'xxx', type: 'single' } })`
   - 页面标题显示：`[题库名称] - [题目类型]`，如："题库1 - 单选题"

### 5.2 查看详情功能
1. **实现方式**：
   - 点击"查看详情"按钮，打开一个对话框（Dialog）或抽屉（Drawer）
   - 显示题目的完整信息，包括：
     - 题目编号、科目、题目内容、题目类型、难度等级、分值
     - 选项（如果是选择题）：选项A、B、C、D等
     - 正确答案
     - 解析（如果有）
     - 知识点、标签、创建时间、更新时间等
   - 对话框/抽屉标题：`题目详情 - [题目编号]`
   - 提供"关闭"按钮

2. **数据结构**：
```typescript
interface QuestionDetail extends QuestionItem {
  options?: string[];        // 选择题选项
  correctAnswer: string;     // 正确答案
  analysis?: string;        // 题目解析
  // 其他详细信息...
}
```

### 5.3 编辑功能
1. **实现方式**：
   - 点击"编辑"按钮，打开编辑对话框（Dialog）或抽屉（Drawer）
   - 使用表单（el-form）编辑题目信息
   - 表单字段：
     - 科目（下拉选择）
     - 题目内容（文本域）
     - 题目类型（下拉选择，不可修改或可修改）
     - 难度等级（下拉选择）
     - 分值（数字输入）
     - 选项（如果是选择题，动态添加选项）
     - 正确答案（根据题目类型显示不同输入方式）
     - 解析（文本域）
     - 知识点、标签等
   - 提供"保存"和"取消"按钮
   - 保存后刷新列表数据

2. **表单验证**：
   - 必填字段验证
   - 选项数量验证（选择题至少2个选项）
   - 分值范围验证等

### 5.2 标签页管理
- 详情页应该在标签栏中显示为新的标签
- 标签名称：`[题库名称] - 题目列表` 或类似格式
- 关闭标签时返回原题库页面

### 5.3 面包屑导航
- 显示路径：`首页 / 原题库 / [题库名称]`
- 支持点击面包屑返回上一级

## 六、样式要求

### 6.1 页面样式
- 使用与原题库页面相同的样式风格
- 保持整体布局一致性
- 使用相同的颜色主题和间距规范

### 6.2 组件样式
- TableToolBar 组件样式保持一致
- 表格样式与原题库页面相同
- 按钮、输入框等组件样式统一

## 七、实现步骤建议

### 阶段一：基础功能
1. **创建文件结构**：
   - 在 `src/pages/originalQuestionBank/` 下创建 `detail` 文件夹
   - 创建 `detail/index.vue` 文件
   - 创建 `detail/components/` 文件夹
   - 创建 `questionType/` 文件夹

2. **配置路由**：
   - 在 `src/router/index.ts` 中添加详情页路由和题目类型页路由
   - 配置路由参数传递

3. **实现页面组件**：
   - 复制原题库页面的基本结构
   - 修改表格列配置为题目相关字段（包含科目字段）
   - 实现搜索和筛选功能
   - 添加返回按钮和面包屑导航

4. **修改原题库页面**：
   - 修改 `handleView` 函数，实现路由跳转
   - 传递题库ID参数

### 阶段二：详情和编辑功能
5. **实现查看详情功能**：
   - 创建 `QuestionDetailDialog.vue` 组件
   - 在详情页中引入并使用该组件
   - 实现详情数据的获取和显示
   - 处理各种题目类型的显示（选择题显示选项，填空题显示答案格式等）

6. **实现编辑功能**：
   - 创建 `QuestionEditDialog.vue` 组件
   - 实现表单编辑功能
   - 根据题目类型动态显示不同的编辑字段
   - 实现表单验证
   - 实现保存功能

### 阶段三：题目类型页面
7. **实现题目类型专门页面**：
   - 创建 `questionType/index.vue` 文件
   - 实现只显示指定类型题目的功能
   - 表格字段根据题目类型动态调整（如选择题显示选项列）
   - 添加返回按钮，返回题库内容详情页
   - 配置路由和标签页管理

8. **实现数据获取**：
   - 创建或使用现有的API函数获取题目列表
   - 实现分页、搜索、筛选功能
   - 实现按类型筛选的功能

### 阶段四：测试和优化
9. **测试功能**：
   - 测试页面跳转（原题库 -> 详情页 -> 类型页）
   - 测试搜索和筛选
   - 测试返回功能
   - 测试标签页管理
   - 测试查看详情功能
   - 测试编辑功能
   - 测试题目类型页面

10. **优化和完善**：
    - 优化用户体验
    - 添加加载状态和错误处理
    - 优化样式和布局

## 八、题目类型专门页面详细说明

### 8.1 页面功能
- **页面目的**：只显示指定类型的题目，提供更专注的查看和编辑体验
- **路由参数**：
  - `bankId`: 题库ID
  - `type`: 题目类型（single/multiple/fill/shortAnswer/judge/essay）

### 8.2 表格字段（根据题目类型动态调整）

#### 所有类型通用字段：
- 题目编号
- 科目
- 题目内容
- 难度等级
- 分值
- 创建时间
- 更新时间
- 操作列

#### 选择题（single/multiple）特有字段：
- 选项A、选项B、选项C、选项D（或更多选项）
- 正确答案（显示选中的选项）

#### 填空题（fill）特有字段：
- 答案格式（显示填空位置和答案）

#### 简答题/论述题（shortAnswer/essay）特有字段：
- 参考答案（显示参考答案内容）

### 8.3 页面布局
- 顶部：面包屑导航 `原题库 / 题库内容 / [题目类型]`
- 返回按钮：返回题库内容详情页
- 工具栏：使用 TableToolBar 组件
- 表格：根据题目类型显示不同的列
- 分页：支持分页功能

### 8.4 数据筛选
- 自动根据路由参数中的 `type` 筛选题目类型
- 支持在该类型范围内进行搜索和筛选
- 搜索主要针对题目内容

## 九、注意事项

1. **路由参数**：确保正确传递和接收题库ID、题目类型等参数
2. **数据加载**：页面加载时根据参数获取对应的题目列表
3. **状态管理**：考虑是否需要使用 Pinia/Vuex 管理题库和题目状态
4. **错误处理**：处理题库不存在、题目列表为空、题目类型不存在等情况
5. **性能优化**：大量题目时考虑虚拟滚动或分页加载
6. **用户体验**：加载状态、空状态、错误状态的友好提示
7. **对话框管理**：详情和编辑对话框的状态管理，避免重复打开
8. **表单验证**：编辑表单的完整验证，确保数据完整性
9. **题目类型切换**：在类型页面中，如果切换类型，应该更新路由和筛选条件
10. **标签页管理**：类型页面也应该在标签栏中显示，标签名称：`[题库名称]-[类型名称]`

## 九、参考文件

- 原题库页面：`src/pages/originalQuestionBank/index.vue`
- TableToolBar 组件：`src/components/tableToolBar/index.vue`
- 路由配置：`src/router/index.ts`
- 布局组件：`src/layouts/mainLayout/index.vue`

